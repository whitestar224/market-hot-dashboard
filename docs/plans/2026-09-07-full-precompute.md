# 全文档标的 v90 本地预计算

用户要求所有币种计算一遍。范围为 83 个币种、85 个独立文档时间段 × 5 个周期（5m、15m、1h、4h、1d）=425 项；不含暂停的1m。

1. 核对当前引擎版本、完整来源窗口与现存任务；保持人工反馈不变。
2. 补齐缺失原始K线并先持久化，逐周期计算落盘；采用完整批模式保证跨周期上下文，不使用优先队列截短结果。
3. 进程使用隐藏窗口、低优先级、独立日志与版本锁；中断后可恢复。失败明确记录，不能作为0B成功。
4. 核对425项完成度、数据缺口、已确认时间K线和原生/人工恢复买点；给出剩余失败列表。

初始只有XRP的5项可完整复用本地原始数据，其余420项需要从配置的公开行情源补齐。数据源可能回退至现货，报告以实际venue为准，不将其误称为永续数据。

本轮不改策略规则、不自动否定确认记录、不下单或播报历史信号。使用Code流程验证执行与服务控制；按回测检查原则区分缺失、过滤、原生命中与人工恢复，不报告收益率。

## 已启动的执行记录

- 2026-09-07 22:14（北京时间）启动独立后台批次；包装进程28992，计算进程28200。不要重复启动同版本任务或打断当前计算。
- 本地数据服务8791已验证可获取历史行情；该进程禁用自动预计算抢占，由本批次顺序处理完整案例。
- 本批次日志：`.runtime-cache/dragon-wave-precomputed/v90-20260907-221537-28992.full.stdout.log` 与同名`.stderr.log`。
- 源K线先保存到`.runtime-cache/dragon-wave-candles`，策略结果保存到`.runtime-cache/dragon-wave-precomputed/v90`。部分周期已落盘不代表完整案例跨周期汇总结束。
- 已验证TUT的1h（882根）、4h（288根）、1d（123根）、15m（3288根）落盘；5m已保存原始行情，进入策略计算。此为启动阶段检查，不是全量完成声明。
- 批次结束后包装进程自动运行只读完整验证；结果见`deliverables/2026-09-07-full-precompute/verification.md`。轻量进度可用`node tools/report_dragon_wave_precompute.js --version=v90`刷新。
- 确认库中本次目录和启用周期范围内有316个去重确认点；只有完整结果校验后才能分别报告原生命中、人工恢复或未保留。
- 执行与报告新增测试21项通过；页面/数据/反馈/报告相关测试100项通过；服务与反馈Python测试21项通过。
- 实测本地读取可返回TUT的已落盘文件，但临时上下文也会得到HTTP一天缓存。因此看板读取改用ETag重验证，显式`contextComplete:false`走原有等待/重试流程且不回退至旧IndexedDB结果。兼容没有该字段的既有缓存；页面测试51项通过。本次展示修复不改变策略引擎哈希，也未打断计算或重启服务。

当前未完成：其余标的运算、完整425项核验及确认点保留汇总。电脑与本地行情服务需保持运行；中断时使用同版本全量批次可从完整案例和原始行情缓存恢复。
