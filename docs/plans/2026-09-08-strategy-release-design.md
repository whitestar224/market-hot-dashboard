# 新策略 v91 安全切换

用户已要求切换刚验证完成的新策略。采用按完整案例发布：v91 独立存储，五个周期计算完毕且引擎指纹一致后启用；未完成时仍显示 v90 完整结果并明确标注旧版。避免直接清空缓存导致 B 点消失，也不等待全币种完成才开放已完成案例。

- 只从已有本地 K 线重算，缺失行情单列，不现场拉交易所。
- 看板一次加载先选定案例版本，五个周期不混用新旧结果；旧缓存不得冒充新引擎结果。
- 新版未就绪时只轻量检查发布状态，不反复清空重载图表。
- 人工确认、否定记录原样叠加，布局不变，不改自动交易配置，不重启交易服务。
- 校验未完成/错误指纹/缺少文件的缓存不可发布，验证真实接口与完整案例，保留 v90 回退文件。
