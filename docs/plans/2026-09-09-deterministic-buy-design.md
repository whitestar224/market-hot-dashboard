# 交易执行去 AI 与并行预读

用户要求：可明确写成规则、计算的动作全部用代码，仅新闻理解等必要判断使用 AI。追加到原需求，不覆盖解释推文、事件流、速度验收等未完成项。

选择：移除跨链选路的模型依赖，安全校验通过且未过期的候选按最低到账/总付款费用排序，同等条件优先较快路线。校验记录绑定最终报价哈希，授权时重验。AI 不参与余额、换算、授权、构造、签名或广播。

并行读取 USDT 价格、合约风险、目标链余额；余额与原生币价格也并行读取。使用独立有界只读池，避免嵌套任务等待同一池导致死锁。现有价格 15 秒、余额 20 秒缓存时效不放宽；不把稳定币价格硬编码为 1，不隐藏接口错误，不跳过安全检查。

不选择：保留模型但缩短超时（仍可能拖慢）；取消报价/最低到账检查（不能解决网络耗时，且改变价格保护）。20% 上限暂保留，最新取消滑点保护的含义仍未确认。

验收：模型完全不可用仍可生成同链/跨链只读报价；过期、篡改、混币混金额候选拒绝；授权必须匹配规则记录；独立读取同时启动、风险阻止时不构造交易；线程池无相互等待；缓存隔离/精度/幂等/签名测试回归。上线与实测耗时、真实签名验收分别记录。

本轮结果：183 后台测试、72 页面测试通过。在线 4Stock 10 USDT 两次只读报价24.95秒/18.13秒；后一次前置数据1.83秒，后续主要是币安报价/模拟。不能声称零等待或用不同网络时刻的结果承诺固定加速倍数；未签名、未广播。

用户随后追加“相关推文也算”。已允许同项目背景/相关观点与事件源头，并纳入24字以上的实质短帖；仍校验原帖、具体关联、质量与广告，未把同名日常词当成币。相关帖必须提供原文可核对的关联证据。旧空结果不沿用旧筛选规则缓存，键不变所以老弹窗可重试。

うさぎ：真实点击键c980032a49e336383d4098b9279f8207e13e0602；旧候选0。新版重搜失败在搜索层，独立诊断X直连402额度不足，本地Codex在87.88秒超时。不得把接口失败说成质量不合格；真实内容展示尚未验收，保持待办。错误分类现在给出两条通道的具体安全提示，不泄露凭证/子进程输出。

## 2026-09-09 买入交互与资金优先级追加

最新要求覆盖此前“同链原生币优先”：付款顺序改为同链稳定币 → 同链原生币 → 其他链稳定币 → 其他链原生币。稳定币余额足额但原生币不足 Gas 时仍拒绝该路线，不放宽燃料费检查；同链均不可用时继续使用现有 Relay 跨链路线与确定性复核。

输入有效 USDT 金额后，主“买入”按钮代表一次明确交易意图：复用或等待精确金额报价，然后直接进入当前全局默认钱包的交易确认。默认钱包尚未连接时，只有这次明确点击可以先请求连接并继续；后台预读与自动报价仍不得请求钱包权限或签名。重复点击、账户变化、关闭窗口和失败重试继续受原有单次发送与失效规则保护。

实现回归：50 个页面/钱包测试、49 个后端买入测试通过；未请求真实钱包签名、未广播、未验证主网到账。
