# 未完成需求与验收记录

新消息默认追加，不覆盖未完成的旧需求。代码修改、测试通过、服务上线、真实成交必须分开记录；没有验证依据不能标为完成。

2026-09-09 全历史核对见 [需求审计](2026-09-09-requirement-audit.md)。新增明确未闭环：1分钟首判实时积压、卖出/普通兑换、未支持链、LUNA身份与发现时间、真实窗口和交易链路验收。不能只跟踪下面的最近需求。

## 本轮必须一并交付

### 9/19 追加：停播 Ave.ai 热门榜新进

- [x] Ave.ai 综合榜和专链榜继续刷新、展示并保留研究数据，但新进入榜不再生成桌面弹窗或语音播报。
- [x] 静默规则同时应用于事件生成和统一送达边界；升级前已排队的 Ave.ai 热门榜新进消息也不能在重启后补弹。
- [x] Ave、通知偏好与行情播报三组共 85 项回归通过；正式服务 supervisor 28976 / backend 30060，restarts=0、failedProbes=0，在线存活接口正常，运行进程启动时间晚于本次 `server.py` 修改时间。

### 9/9 16:42 后追加：授权去向、手机确认与一次点击买入

- [x] 已核对截图中的实际 4Stock 报价：目标 CA 为 `0xd270d4e1ec6e6e0d28c0ecb8be966ec75997ffff`；USDT 授权对象 `0xb44446b0c8e56988c34f7ff73ae904982b5fdda5` 是同一报价内的执行路由，不是买入目标。授权金额等于本次输入，最终收币地址等于当前钱包；当时订单没有交易哈希，链上现有授权额度读数为 0，不冒充已成交。
- [x] 报价卡新增“授权与买入去向”核对区，明确展示付款币 CA、授权对象、仅本次授权额度、执行合约、目标 CA、收币地址和实际 DEX 路径；展示内容与后续完整交易再次绑定，任一项变化会在唤起钱包前停止。
- [x] 点击买入后本站不再增加二次确认，直接准备报价并唤起当前全局钱包；钱包自身的签名确认不能由网页绕过。Binance Wallet 若把签名发送到移动 App，界面会说明这是当前钱包的关联确认模式，并提示可切换到支持本机签名的已连接钱包。
- [x] 已另做“钱包也免确认、点击后直接成交”的独立模式：采用免费 Safe + Zodiac Roles + Windows 本机加密保护的会话账户；包含一次性启用、单笔/累计额度、24 小时有效期、固定合约与参数白名单、自撤销和本机密钥保护。开发与上线测试均未部署合约、未转入资金、未创建会话密钥、未授权或广播真实交易。
- [x] 用户确认独立智能账户方案；最新风控额度覆盖前值，调整为单笔 1000 USDT、每日 10000 USDT、授权 24 小时；跨链第一版继续逐笔钱包确认。额度必须由链上权限和本地规则双重执行，不能只做前端显示。
- [x] 用户选择不接入 ZeroDev 等付费 ERC-4337 打包器；当前免费方案不产生月费或按次服务费，只保留一次性设置和实际交易所需链上 Gas。未来如改用付费服务仍须另行确认费用。
- [x] 免费方案已按 `2026-09-09-free-session-buy-design.md` 实现并上线：Safe + Zodiac Roles，标准交易由本机会话账户支付链上 Gas。链上权限核验固定稳定币授权、币安路由函数、Safe 收币地址、会话成员和 10000 USDT 不补充总额度；本地再执行单笔 1000 USDT、24 小时累计 10000 USDT。每次报价和执行前都从官方 Roles 索引读取最新权限，并用链上允许/拒绝探针复核；索引延迟、网络异常或任一参数不符均拒绝执行。24 小时由本地停用并自动发起链上自撤销，电脑离线时未用链上额度仍存在，下一次启动后继续撤销。
- [ ] 真实启用仍需用户在页面选择“免费免确认”，创建本机会话账户，并在 Safe/Zodiac 做一次性权限设置和钱包确认；未完成这一步前保持未启用。不得由开发测试代替用户创建账户、签名、转入 Gas 或真实买入。
- [x] 本模式最终定向回归：后端 63 项、前端 44 项全部通过；在线服务 supervisor 16764 / backend 42612，restarts=0、failedProbes=0，页面加载 `monitor-buy.js?v=18`。在线状态返回 `keyCreated=false`、`configured=false`、`active=false`、1000/10000 USDT，磁盘确认无会话密钥文件。
- [x] 9/9 19:35 截图复现普通钱包模式仍显示旧单笔上限 200 U：根因是旧后台进程继承旧配置且未真正重载。已受控停止旧守护并重新启动，新在线接口与会话模式均返回单笔 1000 U；页面加载 `monitor-buy.js?v=19`。
- [x] 同一截图组出现 1 U 报价改扣 BNB、Binance Wallet 模拟 `execution reverted`。订单记录确认钱包有约 242 USDT 同链余额，失败订单没有交易哈希。用户随后明确：稳定币只是优先，稳定币路线失败时允许改用同链原生币。最终规则为先逐一尝试同链稳定币，失败后再尝试同链原生币，并在报价顶部和明细卡显眼说明“稳定币路线不可用，已改用 BNB”；不允许静默切换。钱包前保留服务端独立链上试运行和钱包节点试运行，任一回滚均在发起钱包交易请求前停止。
- [x] 最终修复定向回归：后端 72 项、前端 45 项全部通过。在线 supervisor 35656 / backend 25532，restarts=0、failedProbes=0；页面加载 `monitor-buy.js?v=21` / `styles.css?v=113`，普通与会话单笔均为 1000 U，会话累计 10000 U。此前用截图同一钱包、同一 BSC 4Stock CA、1 U 做的只读实测选择 BSC USDT `0x55d398326f99059ff775485246999027b3197955`，未唤起钱包、未授权、未广播交易。
- [x] 1 USDT 等小额只读报价遇到网络失败时自动重试一次，不重试签名或交易；仍无路线时明确提示小额可能暂不满足成交/费用保护，绝不擅自把用户金额改成 10 USDT。
- [x] 本轮买入、授权展示、小额重试及股票弹窗静默的定向回归：Python 118 项、前端 41 项通过。受控更新后守护 38256、后台 11924，restarts=0、failedProbes=0；价格监控与事件流均加载 styles v109 / core v6 / controller v15，能力接口保持同链 Binance Web3、跨链 Relay。更新前后最近 10 分钟均无订单或交易哈希，未触发钱包授权、签名或广播。

### 9/9 15:49 后追加：屏蔽巨鲸/机构增减持及股票热门榜弹窗

- [x] “巨鲸/机构/公司增持、减持、加仓、减仓、持仓升降”等持仓变化类快讯不再进入桌面弹窗和语音；截图中的 CleanSpark 减持标题已作为回归样例。
- [x] 过滤位于共享桌面通知边界，只阻止弹窗与播报，不删除聚合快讯页面内容；价格监控产生的真实买点/突破信号保持优先，不因正文提到巨鲸而被误伤。
- [x] 送达调度前再次应用过滤，升级前已经持久化但尚未显示的同类消息会被标记为 suppressed，不会在服务重启后补弹。
- [x] 港股、美股、A股的热门榜、涨幅榜、成交额榜及普通公司公告均不再生成桌面弹窗和语音；截图中的“港股热门榜新进：联想集团”已作为回归样例。网页榜单数据暂不删除，只在用户主动打开时展示。
- [x] 三类股票榜单同时在源头监控与持久化送达边界静默；旧消息即使没有来源编号，也会按股票市场标签与榜单类型识别，升级前已排队的同类通知不会补弹。加密货币榜单、价格买点和项目公告保持正常播报。
- [x] 扩大后的静默规则已纳入本轮 Python 118 项定向回归并随后台 11924 受控上线；守护 38256，restarts=0、failedProbes=0。此前 194 项与后台 33860 仍只代表上一版，不能混作本次验收依据。

### 9/9 14:00 后追加：全网热点 Meme、Ave 专链榜与研究入池

- [ ] News Trade 每小时调用一批现有本地 Codex 联网搜索，主动发现当前新闻源遗漏的全网热点事件；每天最多 24 批，失败也计数，X 官方付费 API 保持关闭。调度、持久计数和每日上限已上线；首批在模型请求前被用户目录里的损坏技能阻断，仍按要求计为第 1 批。现已改为只复制本地登录凭证到一次性隔离目录，`codex login status` 验证通过；不得突破一小时一次限制手动补跑，下一批 2026-09-09 15:52:25 自动实测后才能闭环。
- [ ] 热点事件通过现有链上搜索匹配 Meme；只有链、CA 和交易数据可核验的候选进入链上候选池。Jacob Coxon 离职与同名 Jacob Meme 作为验收场景，不能写死为唯一事件。
- [x] 扫盘和链上投研遇到与活跃热点强绑定的盘子时加入研究；只有通过硬风险、最低流动性和活跃度校验并经 AI 复核后才能进入“精选”。候选增强器在量化筛选后运行，热点只能把满足最低流动性/活跃度的候选提升到待 AI 复核，不能直接写入精选；专门回归通过。
- [x] Ave.ai 保留综合热搜前十，并新增 Robinhood、SOL、ETH、BSC、Base 五个专链热搜前十；五链条目全部进入扫盘和链上投研。线上总接口与独立缓存均核对为 10/10/10/10/10/10；链上研究库已出现 robinhood/solana/eth/bsc/base 的 `ave-hot` 候选与 ready/pending 状态。
- [x] Ave.ai 与富途美股热门榜卡片交换位置；Ave 卡片内使用综合/五链切换，切换不重复请求接口。线上源顺序为 `futu-hk, ave, ths-cn, futu-us`。
- [x] Ave 时间窗口只保留 1h、4h、24h，默认 4h；三个周期的涨跌、成交额和交易笔数随同一次榜单响应下发，页面本地即时切换，不追加接口请求。线上响应核对 `period=4h`、`periodOptions=1h,4h,24h`。

### 9/9 最新追加：成本控制、增量回填与中文解释兜底

- [x] X 官方 API 继续保持代码级关闭，环境变量和旧密钥不能自行打开。若未来经用户明确批准，只允许个人账号：先读累计发帖数，再按相对上次完整基线的实际增量读取，不固定拉 80 条；引用推文不进入监控。每日费用硬上限 0.10 USD、请求硬上限 300 次，其他 KOL、项目方及 Aster 禁止走付费路径。
- [x] 解释推文仅保留中文正文；本地 X 搜索快捷入口固定带 `lang:zh -is:retweet`，英文检索结果不进入阅读窗口。打不开的中文相关原帖仍保留安全的具体链接。
- [x] 一次本地 Codex 请求同时完成中文推文搜索和结构化中文解读。没有可读推文时，窗口展示“AI 中文解读 · 非 X 推文”，回答标的、起因、经过、结果、影响与待核对项，不再为了兜底启动第二次模型请求。
- [x] 解释任务使用独立轻量配置：默认 `gpt-5.6-luna`、`minimal`、60 秒；不会启用新的收费 API。已明确记录 Codex CLI 只是本机入口，仍消耗已登录 Codex 账号额度，不冒充免费离线模型。
- [ ] 真实联网速度与中文命中率仍需部署后的实际事件观察；单元测试不能证明每个冷门标的都能找到中文原帖。X 付费路径保持关闭，因此不能用付费接口提高命中率。

### 9/9 12:35 后追加：快讯核心语义去重与首次 Meme 入池

- [x] 同一快讯不再以完整标题作为唯一去重依据；来源消息 ID + 原发布时间形成稳定身份，标题微调仍视为同一条。旧历史 seen 键启动时补齐稳定身份，持久化送达库在命中旧别名时回填新别名。
- [x] 跨来源去重先按主体、动作、标的、数字和时间做快速筛选；歧义候选使用现有 AI 一次批量判断“核心意思是否一致”，结论持久缓存。同义改写合并，主题相关但不是同一事件不合并，动作相反或关键数字冲突保留。用户已在本轮明确要求调用 AI；未启用 X API 或新的第三方接口。
- [x] 律动与吴说各只保留一个直接来源，不再重复读取其 Discord 镜像；同样移除方程式新闻的重复镜像。线上来源列表核对：BlockBeats 律动、吴说区块链、方程式新闻各 1 个。
- [x] 快讯首次提及 Meme/代币且带明确代码时，立即生成 News Trade“事件观察/研判中”条目；完整发现、CA 候选与 AI 研判继续在后台覆盖，不额外生成第二个弹窗。ZZZ 在线核对已存在，AI 状态 ready，层级 event-observation。
- [x] News Trade 事件流写入移出 HTTP 响应路径，快讯手动刷新改为 stale-while-refresh；相关 156 项回归及 14/13 项收尾回归通过。当前手动后台 39116 已加载新代码并通过 liveness；守护因该端口已占用保持停止，未强制结束用户启动的进程。

### 9/9 10:28 后追加：提醒声音、钱包重复上榜、解释链接与CLI

- [x] 用户最新明确取消解释推文的质量/大V/同事件/时间门槛：现在本地Codex搜到的结构有效X原帖或链接直接展示，保持搜索顺序，不再启动额外质量复核。仅保留真实X status链接格式和“摘录/未读正文”来源标识，避免伪造原文。
- [x] 费用路径再次确认：解释搜索不读取X token、不调用X API；点击结果只由浏览器打开X链接。若未来拟在点击后用X API补正文，必须另行估算并取得用户确认。
- [x] 最新放宽规则后273项相关回归通过并重新部署：守护26572、后台26508，restarts=0、failedProbes=0；事件流页面200。部署前后最近10分钟均无订单或交易哈希，未触发钱包授权、签名或广播。

- [x] 不同桌面弹窗共用一个跨进程语音通道；原来的 Windows `SystemNotification` 提示音保持不变，提示音与整段语音都在同一通道内依次播放。两个独立进程的静默临界区测试通过；没有实际发声骚扰用户。
- [x] “补涨映射更新/新龙头”统一在弹窗接纳和送达前静音，不影响页面事件流。专门策略测试通过。
- [x] 币安钱包4小时榜“新进”新增7天上榜记忆：最近7天出现过，即使掉榜后重回也不再算新进；按链+CA识别，不因改名重复提醒。迁移旧提醒/当前榜单记录，5个专门场景通过。
- [x] 解释推文改为一次本地 Codex CLI 同时搜索和筛选，取消应用原先45秒提前截断与空结果后再启动第二轮CLI；后台仍有90秒进程故障上限。用户打开窗口时排队任务提到队首，同一链+CA+事件内容的30分钟结果直接复用。
- [x] X页面打不开时保留具体原推文链接，醒目标为“正文未核验”；搜索索引已读到的正文可作为“搜索索引摘录”展示，不冒充完整原文。窗口同时给出按标的/CA打开X搜索页的即时入口；不调用X API，也未新增收费服务。
- [ ] 真实内容命中率仍需线上观察：改造前对うさぎ的一次本地CLI实测约41.92秒且0条，新版“保留不可读链接/索引摘录”尚不能保证每个标的都有结果；不能标记为已找到高质量推文。
- [x] 本轮271项后台/桌面回归通过；受控更新后守护14004、后台41204，restarts=0、failedProbes=0，价格监控与事件流页面均200。更新前后检查最近10分钟均无订单、无交易哈希；未触发钱包授权、签名或广播。

### 9/9 最新追加：执行去 AI + 相关推文

- [x] 用户追加费用限制：停用所有X官方直连（搜索、时间线、流、规则），连oEmbed也不调用；旧token/环境开关不能覆盖禁用。解释搜索及筛选都仅用既有本地Codex，不回退付费远程模型。长期偏好和项目费用政策已保存；新收费功能一律先确认。
- [x] 相关推文现在允许项目背景、事件源头、相关观点及短帖；本地Codex改为每批一个查询、最多3个原帖，单批45秒，首批成功即返回，失败只换一个方向。已找到摘录在筛选前持久化；筛选有独立通道，失败重试不重新搜索。真实小批42.07秒返回，无超时但0条可读摘录，仍不能标记“已找到”或承诺不再发生任何网络超时。
- [ ] 真实内容验收：うさぎ目前未取得可展示原帖。旧X额度不足已通过禁用彻底停止继续调用，不能以收费API作为恢复方案；仅本地Codex模式继续验收。旧版服务占端口已核验工作目录与无近期交易后停止，只更新本项目服务。

- [x] 交易路线改为纯代码规则选取；同链/跨链执行不调用模型。并行读取价格、余额、合约风险，原有价格时效/金额/收款/最低到账/模拟保护保留。初步54交易测试通过；完整回归/上线/实测速率待记录。
- [x] 解释推文扩大为解释或相关内容，项目背景、原始事件、大V相关观点和有实质内容的短帖纳入；不要求均为同一播报事件。高质量、真实原文、明确关联及去广告去同名误配仍保留。代码及初步筛选回归通过；用户“うさぎ”记录确认为c980032a49e336383d4098b9279f8207e13e0602，旧缓存0候选，待新版重搜验收。
- [ ] 最新取消滑点保护的含义仍待确认；本轮保持20%保护，不用放宽风险冒充提速。此前历史未完成项仍继续保留。

- [x] 监控买入：同链报价/交易构建改用币安 Web3；失败不得回退到旧同链交易。（9/9 00:52 在线 provider、页面版本与 bundle 校验通过。）
- [x] 用户已选择跨链继续使用 Relay，保留 AI 复核和原有硬校验。（新旧通道回归通过。）
- [x] 保留非托管、全局钱包、每笔钱包确认、USDT 金额换算和精确授权。原5%限制已被9/9用户明确选择的“自动滑点最高20%”替代，旧订单仍按当时确认的限制执行。
- [x] 新订单明确区分报价、授权、买入提交和实际到账，授权成功不得显示买入完成。（专门模拟通过；真实资金成交未测试。）
- [x] News Trade 解释推文入口不得移除；热门榜新进、此前没出现过的标的也必须有入口，不能漏掉币安钱包 4 小时热榜。（上下文随新版后台上线；旧弹窗不补造新按钮。）
- [x] 解释推文按同一标的/事件、解释质量筛选，优先高质量大 V 内容；不足三篇不得捏造。独立居中大窗口，默认三篇，最多五篇。（筛选/3→5 展开回归通过，实际内容不足会提示。）
- [x] 验证弹窗布局真实可见、入口可打开、检索不阻塞实时播报。（实际 Tk 控件零透明度布局与启动参数测试；未发送测试播报。）
- [x] 完成回归、构建、检查未完成交易、受控更新服务并验证存活。（143 后端、64 前端通过；pid15180，failedProbes0。）

## 已知边界，不能冒充完成

## 9/9 追加需求（不能覆盖上面事项）

### 9/9 本轮修复（后续于下方历史验收）

- [x] 查清币安 40001：实际接口禁止 autoSlippage 与 slippagePercent 同传。已改为互斥参数，保留 maxAutoSlippagePercent=20；真实只读自动报价成功（5.12%，后续完整构建/模拟2.16%）。之前“自动20%已通过”只验证了模拟参数，不能作为真实接口验收。
- [x] 拆开 Relay 的缺少容忍度、容忍度超限、最低到账不足与无效数据；错误列明供应商及付款链，不混称实际亏损。同一源链本次真实2000/500bps报价均正确；历史截图具体哪条跨链失败仍不可还原，不扩大20%硬限。
- [x] 输入金额即可点击买入：450ms后台精确报价、点击复用同一在途请求，未准备好仍要等待实际检查；改金额/关闭/换钱包取消待执行意图，自动报价绝不签名。悬停/聚焦/打开预备已核验标的的安全信息和USDT价格。只在同一不可变报价模拟成功后10秒内复用模拟，不续期，授权后重新模拟。
- [x] 标题右侧完整CA、可复制、窄屏换行；失败只显示一处。已在真实本地页面查看截图，确认CA完整；输入10后买入按钮可用，关闭正常；未点击最终买入。
- [x] 解释窗口不再吞掉HTTP/连接错误；保留标的标题并从弹窗传入；队列接纳时立即保存原事件，重启前尚未开始的任务也可恢复；新增本地解释点击/打开/重试记录。
- [x] 核查最近点击：旧版关闭、查看与解释共用read回执，不能严格断定具体按钮。最新有解释入口的已读条目AKE（02:46:47）；旧服务请求日志最后可辨认的用户阅读GET为OTC（02:26:13），排除本轮自己的ZCAT探测。不把这两种记录混为“确定最后点击”。AKE原事件可恢复，实际重试0.78秒进入pending，随后60候选筛出2篇，状态ready。
- [x] 本轮回归174后端、72前端通过；包含非托管、准确金额/CA/上限、过期/账户切换/无重复发送及原事件流/解释功能。真实BNB→4Stock完整报价+资金模拟13.81秒，剩余报价寿命46.19秒，未签名/授权/广播。
- [x] 上述后台修复已部署：守护22100/后台22608，restarts0/failedProbes0。线上prepare返回queued；线上BNB→4Stock报价provider=binance-web3、自动滑点2.03%、未发钱包请求。price-watch/event-flow均加载controller v12/styles v107；AKE重启后ready/2篇。
- [ ] 提速实效尚未达到零等待：完整线上冷报价40.43秒，随后分段复测16.84秒（USDT价格5.71秒，余额等6.08秒，币安构建模拟4.86秒）。不能拿单独构建13.81秒或按钮可点击冒充全流程瞬时成交。钱包最终签名和真实成交仍待用户验证；U03/U04/U06及原生解释窗口截图验收仍保留。
- [ ] 最新追加：用户要求“直接忽略滑点，按最新市价买进”以节省报价时间。已解释实测耗时不是滑点运算，取消保护不等于免报价。需确认是免手动滑点设置还是取消20%价格保护；本轮没有静默改成无限滑点/零最低到账，也未交易。

- [x] 解释推文按钮从底部整条移至弹窗顶栏“查看”左侧；Tk真实控件布局与启动绑定回归通过。
- [x] CATE解释窗口失败可恢复：原始上下文和已搜索原文持久化、独立重试按钮。最新线上GET返回empty/27条候选已筛选；本轮无通过同一身份/事件质量核验的原文，不编造三篇。
- [x] 本地Codex联网补搜解释推文：仅允许公开搜索，隔离临时目录、禁用shell/插件，不提供应用凭据；只接受X原文接口核对后的真实文本。实际CLI执行完成约70秒，CATE本次未找到URL；X直连约12秒取候选，所以采用直连优先、Codex失败补搜，不承诺每个币必有合格结果。
- [x] 阅读窗口尽量像X原文：作者/账号在上、正文主体、白底黑字、次要阅读提示置底；不伪造头像、蓝标或互动数。代码和Tk展开测试通过，已上线。系统截图工具报Windows接口不支持，真实截图视觉验收继续归U07，不能宣称已截图确认。
- [x] 自动滑点：用户明确选择20%上限；币安autoSlippage=true且maxAutoSlippagePercent=20，前后端及calldata最少到账再次核对。回归通过、在线能力slippageBps=2000/autoSlippage=true，不自动重发交易。
- [x] Gas误拦：新报价从固定报价3倍改为已展示的独立费用预算；计入后续授权和买入Gas。失败发生在发送前时明确显示未发起交易请求。代码/模拟通过、bundle线上哈希一致，实际钱包待用户验证。
- [ ] 首轮研判调度与Codex旧模型兼容修复：队列分道及测试已完成，真实一分钟目标未验收，继续保留U03。
- [x] 小白买入：输入USDT后900ms自动准备只读报价，全局钱包被动读取，不自动申请权限或签名；设置/路线明细折叠。完整前端批次68项通过，最新controller v11/core v5/styles v105均已线上核对。
- [x] 核查币安跨链开放情况：官方钱包确有跨链，但公开Web3 API尚未找到可调用跨链构建端点，保持先前授权的Relay，不静默改用私有接口。

本轮最终主回归：165后端通过、68前端通过。最近10分钟无新订单后受控重启，线上pid26564/守护840，restarts0/failedProbes0；executor v6哈希一致。本轮没有发起任何真实授权、签名或广播。

### 9/12 精选推荐翻页与质量排序

- [x] 修复“下一页像点不动”：点击后立即显示目标页加载状态并锁定重复点击；请求失败会恢复原页和地址栏页码，不再静默无反馈。当前页写入地址栏，刷新后仍停留在该页；切换日期或公链时页码归零。
- [x] 精选标的统一按“正式 AI 推荐、结论强度、证据完整度、叙事强度、置信度、量化分、流动性/成交”从优到次排序。生成时和分页响应前各排序一次，旧缓存也不能保留过时乱序；待 AI 补全的新闻触发项排在正式 AI 精选之后。
- [x] 历史 2026-09-09 在线验收共 39 个、4 页：第 1 页为 CATE(叙事66) 起，第 2 页为 WC(叙事28) 起，页码分别返回 1/2 且内容不同，跨页顺序连续下降。当前 9/12 只有 5 个候选，因此正常只有 1 页。
- [x] 83 项链上后端测试及 29 项页面/实时刷新回归全部通过；部署前最近 10 分钟无订单、交易哈希或免确认账户占用。新版已加载为 price-watch.js?v=86，守护 9364、后台 27516，restarts=0、failedProbes=0；本轮未发起钱包授权、签名或交易广播。

- 没有进行真实资金交易；不测试钱包签名、授权或广播。
- 币安当前已验证的同链执行适配范围为 BNB / Ethereum / Base 的固定路由与调用。未知路由、RFQ 与 Solana 同链执行先明确拒绝，不能虚称全部支持。
- 此文档不是此前全部历史需求的完成证明；用户追加或指出旧需求时必须保留并补充验收，不能因新消息自动丢弃。

### 9/9 其他钱包资产与跨链兜底

- [x] 普通钱包付款顺序扩为六级：同链稳定币 → 同链原生币 → 同链其他可兑换币 → 其他链稳定币 → 其他链原生币 → 其他链其他可跨链币。前级不足或路线失败才继续。
- [x] 其他币只来自 Relay 当前 `/chains` 目录且 `supportsBridging=true` 的条目，每链硬上限 24 个；不依赖付费资产索引器，不扫描任意空投币或用户输入的陌生付款合约。
- [x] ERC20 跨链授权继续在服务端收窄到本次确切扣款额；报价仍绑定付款币、链、钱包、收款地址、目标 CA、最低到账和协议收款合约。
- [x] 免费免确认 Safe 权限不随本需求扩大，仍只允许预设同链稳定币；其他代币及跨链仍由普通钱包本人确认。
- [x] 83 项相关后端测试、46 项前端测试及 Python 语法检查通过；未连接钱包、未授权、未签名、未广播。已受控更新：守护 5732、后台 37076，restarts=0、failedProbes=0；liveness 200。线上能力接口单笔上限 1000 U，BSC/Base 已下发 stable/native/other 分类，price-watch 与 event-flow 均加载 monitor-buy v22。

### 9/12 服务启动时重新连接 AI

- [x] 每次后台服务启动都会清除上一进程留下的 AI 冷却状态，并在后台发起一次极小的真实 Codex 请求；探测失败只将 AI 标为暂不可用，不影响行情、规则分析及网页服务继续运行。
- [x] 已打开的榜单在 AI 暂不可用时停止重复消耗额度；检测到服务进程已经重启且本次启动探测成功后，自动重新请求 AI 分析，无需反复手动刷新。
- [x] AI 恢复后只补跑最近 24 小时内评分最高的 24 条精选候选。验收中发现旧实现误将 2433 条一起入队，已立即停服并将其完整保留为等待状态；修正后真实启动显示 provider=codex-cli、requeued=24。
- [x] 46 项后端 AI/链上研究测试、8 项前端 AI/榜单测试、Python 与 JavaScript 语法检查通过；服务真实启动 pid 37676，启动探测状态 ready。本轮未发起钱包授权、签名或交易广播。

### 9/12 结构与前高漏报修复

- [x] 查明页面识别与桌面弹窗原来存在两套门槛：币龄/回撤等“质量资格”、来源文字匹配、预备结构阶段、首次快照及重启断档均可能让已经显示的结构静默。ZHONGJI 的持久状态也确认曾进入前高区，但 `last_alert_at=0`，属于真实漏报而不是用户错觉。
- [x] 只要标的仍属于当前监控池、对应周期已开启且识别出有效结构，就允许首次结构弹窗；质量结论只参与展示和排序，不再拦截。预备起爆也视为已识别结构，同时保留正式买点与临界预警的独立高优先级提醒。
- [x] 新增“从未成功播报”的自愈：首次快照、服务重启或行情断档后，当前仍成立的结构/接近前高/突破前高会补播一次；已经成功播报过的旧事件不会重放。若弹窗队列未接纳，不消耗播报机会，下轮可重试。
- [x] 修复送达队列的第二道误拦截：结构、预警和正式买点不再被当作“过期前高”；前高事件排队期间若数据库确认条件仍成立，也不会仅因等待超过实时窗口而被删除。价格已回落、来源已退出或手动剔除时仍会取消。
- [x] 保留手动周期开关、全局剔除、新币池仅 1h/4h、同一标的一小时结构去重及前高状态机的退出/重入规则。165 项后端针对性回归、16 项前端/事件队列回归和语法检查通过。
- [x] 部署前后最近 10 分钟均无买入订单；未发起钱包授权、签名或交易广播。新版由守护进程运行，后台 pid 1540，restarts=0、failedProbes=0。此前被旧队列误删的 ZHONGJI 当前接近前高提醒已按新规则重试，送达记录为 displayed、实际可见约 12.5 秒。
