# 未确认 B 点的离线复盘与窄范围优化

## 范围与保护

- 审核自动 B，区分人工确认、否定、待定、从未复核；不更改人工反馈数据库。
- 不把未确认等同于差，不用突破后的涨跌反向定义结构质量。
- 保留主升浪短盘整例外、独立多周期共振，以及用户要求的 1h/4h 有效结构突破。
- 使用 Code 的分步实施/验证流程及 backtest-expert 的因果性、样本覆盖和偏差检查；本次不是收益率回测。

## 实施步骤

1. 合并本地反馈，查清行情及自动信号存量。输出来源、覆盖区间和缺失列表；不冒充全量。
2. 离线重放现存真实行情，逐条输出未确认 B 的证据和薄弱项，并保存修改前基线。
3. 优先修复可复现的结构证据错误。没有真实样本验证的阈值改动只列建议，不直接全局部署。
4. 对照修改前后信号，检查已确认点、已否定点、跨周期与历史结构回归；更新共用引擎及版本引用。
5. 输出具体案例、原因、变化数和验证边界。不得把人工补回的 B 计为策略原生命中。

## 初步数据约束

2026-09-07 检查时，`dragon-wave-precomputed`、`confirmed-native-audit-candles`、`strategy-issue-candles` 三个本地目录不存在。反馈 DB、XRP 完整审计行情和 10 组行情测试文件仍在。
当前工作区另有未提交的清理脚本将上述三个目录列为删除目标。本次不执行清理、不覆盖该脚本、不删除缓存。
若无法找到完整数据，本次仅交付可验证范围的审计与修复，清楚列出未完成的全量部分。

## 本轮完成状态

- 完成 14 组同范围真实行情的修改前/后对照：全部原生 B 151→147，文档日期内未确认 B 101→97；减少 4 个、未新增。
- 修复前高空值伪证据、紧凑补充分支借用内部局部前高、有效三角附带错误横盘标签。未增加统一根数或量能过滤，没有修改人工反馈。
- 覆盖 20 个已确认点：原生 16→16，显示 20→20（4 个仍为人工恢复）；覆盖 1 个否定点，两版均不生成原生 B。
- v90 引用已在共享引擎的看板、预计算与监控入口同步；没有重启服务或声称全部预计算已重建。
- 270 项 JavaScript 策略测试、18 项 Python 反馈测试通过；语法、差异空白检查通过。独立代码复核没有发现阻断问题。
- 交付详见 `deliverables/2026-09-07-unconfirmed-b-audit/复盘结论.md` 与逐条清单。
- 未完成全量部分：缺失的其他历史行情/预计算数据，以及本轮未完成的 XRP 全长 5 分钟回放；不能将局部结果外推成全币种审计完成。
