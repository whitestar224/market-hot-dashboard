# 费用与外部服务授权

用户于2026-09-09明确要求：不使用X直连接口；凡新功能可能花钱，必须事先说明并获得确认。

- 新增或启用收费API、模型服务、付费备援、订阅/套餐，先说明用途、可能的计费方式或费用不确定性，等用户确认后再实施。已有密钥、可调用接口、要求提速均不等于费用授权。
- 当前X官方API全局关闭；旧环境变量和留存凭证不能覆盖用户禁用。不得自动启用官方搜索、用户时间线、流式订阅或规则管理，也不调用oEmbed。
- 解释/相关推文仅使用用户已同意的本地Codex检索与筛选，不自动转向付费X或远程模型接口。本地Codex消耗现有账号额度，不承诺无限免费；若需要购买、充值或使用新的付费方式，仍须先确认。
- 本地Codex检索摘录须标明来源方式，不能冒充X接口核验全文，不伪造作者、粉丝数或认证标记。
- 已有其他功能不视作对新收费渠道的授权；任何扩展仍遵守先确认。

## 个人X监控的成本防线

- 付费路径在代码与环境配置两层均默认关闭，留存密钥不会自动恢复请求。
- 若日后获得明确授权，付费目标仅能是 `X_KOL_PRIORITY_HANDLES` 的第一个个人账号；环境变量不能单独扩大到全部KOL。
- 历史补漏先读取并比较持久化的账号发布总数，仅用 `since_id` 拉取增量；首次只建基线，不回放固定80条。
- 补漏结果过滤引用推文，不请求被引用原帖及其作者的 `expansions`。
- 本地按UTC日记录请求数、Post资源和User资源。总估算费用硬上限为0.10 USD/日，请求硬上限为300次/日；环境配置只能调低，不能调高。
